/**
 * Application abilities for Bouncer authorization.
 * Add abilities here as the project grows.
 *
 * Example:
 * export const editCliente = Bouncer.ability((user: User) => {
 *   return user.roles.some((role) => role.name === 'administrador')
 * })
 */

export {}
